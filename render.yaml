services:
  - type: web
    name: food-detection-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app --timeout 120 --workers 2 --bind 0.0.0.0:10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    plan: free

